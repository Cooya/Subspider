{
  "name": "subspider",
  "version": "1.0.0",
  "description": "Web scraper for collect subtitles",
  "main": "app/Processor.js",
  "scripts": {
    "start": "tsc -p tsconfig.json && node app/Processor.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Coya",
  "license": "ISC",
  "dependencies": {
    "@coya/database": "0.0.1",
    "cheerio": "^1.0.0-rc.2",
    "request": "^2.83.0"
  }
}
